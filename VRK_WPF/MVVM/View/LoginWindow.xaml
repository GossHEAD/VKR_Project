<Window x:Class="VRK_WPF.MVVM.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VRK_WPF.MVVM.View"
        mc:Ignorable="d"
        Title="Авторизация" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid>
        <Border Padding="20" BorderBrush="Gray" BorderThickness="1" CornerRadius="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
                <TextBlock Text="Авторизация" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                
                <TextBlock Text="Логин:" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                
                <TextBlock Text="Пароль:" />
                <PasswordBox Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
                
                <TextBlock Foreground="Red" Visibility="{Binding IsErrorVisible, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding ErrorMessage}" />

                <Button Content="Войти" Command="{Binding LoginCommand}" Margin="10" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
