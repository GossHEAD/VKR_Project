<Window x:Class="VRK_WPF.MVVM.View.NodeSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VRK_WPF.MVVM.View"
        mc:Ignorable="d"
        Title="NodeSettingsView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Node ID:" />
            <TextBox Text="{Binding Node.Id}" IsReadOnly="True" />
            <TextBlock Text="Address:" />
            <TextBox Text="{Binding Node.Address}" />
            <TextBlock Text="Port:" />
            <TextBox Text="{Binding Node.Port}" />
            <Button Content="Start Node" Command="{Binding StartNodeCommand}" />
        </StackPanel>
    </Grid>
</Window>
